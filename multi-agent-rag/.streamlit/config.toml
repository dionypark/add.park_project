[theme]
# 기기(폰)가 다크모드여도 항상 밝은 톤(하늘색 그라데이션 브랜딩)으로 고정한다.
# 안 그러면 Streamlit 기본 위젯(입력창, 탭 텍스트)만 다크모드로 바뀌면서 커스텀 배경이랑
# 안 맞아서 글씨가 거의 안 보이는 문제가 생긴다.
base = "light"
primaryColor = "#5B7089"
backgroundColor = "#FBFDFE"
secondaryBackgroundColor = "#EAF5FB"
textColor = "#2D3B4E"
